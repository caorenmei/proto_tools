# AGENTS.md

## 开发指南

- 使用 `luarocks` 来管理依赖。
- 使用 `busted` 来进行单元测试。
- 使用 `superpowers` 来管理开发流程
- 使用 `EmmyLua` 来进行代码注释、补全和类型检查。

## 强制交互协议

- 总是使用 `AskUser` 来向我提问。
- 完成任务后，使用 `AskUser` 来询问我下一步操作：
    - 当前上下文相关的选项（多选）
    - 先到这里
    - 用户输入
